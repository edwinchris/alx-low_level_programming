#include <stdio.h>
/**
 * main-Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int n;
if (n > 0)
{
printf(n, "%d\n is positive");
}else if(n==0)
{
printf(n, "%d\n is zero");
}else
{
printf(n,"%d\n is negative");
}
