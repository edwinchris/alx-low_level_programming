#include <stdio.h>
/**
 * main-Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int n;
if (n > 0)
{
printf(n, "%d is positive\n");
}else if(n==0)
{
printf(n, "%d is zero"\n");
}else
{
printf(n,"%d is negative\n");
}
